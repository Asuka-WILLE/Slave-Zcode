# 更新记录

## v1.0.0 - 2026-09-15

- 发布首个 V1 大版本；发布字段采用 `1.0.X`，GitHub 提交标签采用 `v1.0.X`。
- 取消 ZCode `3.12.1` 应用版本硬门槛，改为检查任务、会话、模型目录和命令握手所需的运行时接口。
- 版本字段读取失败时保留 `desktop_version: null` 诊断结果；接口缺失时返回 `DESKTOP_INTERFACE_UNAVAILABLE` 并列出具体名称。
- 保留安装目录发现顺序、协议版本检查、命令 ACK、session ID、结果投影和未知结果保护。
- 更新发布 bundle、安装说明、委派 Skill 和兼容性测试；本版本通过 Node.js 24、Ubuntu 和 Windows CI 验证。

## v0.1.0 - 2026-09-14

- 首次公开版本。
- 安装路径改为自动发现：按正在运行的进程、Windows 卸载注册信息、`PATH`、开始菜单快捷方式和常见目录检查，并保留 `ZCODE_INSTALL_DIR` 手动覆盖。
- 提供基于运行时接口检查的本机 ZCode CDP 桌面桥接；3.12.1 保留为历史回归环境。
- 提供任务创建、状态读取、等待、轮次结束后续接、权限逐项处理、停止和列表工具。
- 支持读取 ZCode 已配置的 provider、model 和 reasoning level，并按任务选择模型。
- 加入 SQLite 请求去重、项目占用、租约、文件基线、人工接管检测和未知结果保护。
- 提供打包 MCP 服务、诊断脚本、自动委派 Skill 和 Windows 启动脚本。
